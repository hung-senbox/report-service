server:
  port: "8012"

database:
  active: "mongodb" # or "mongodb"

  mysql:
    host: "localhost"
    port: "3306"
    user: "root"
    password: ""
    name: "report_service"

  mongodb:
    host: "report_db"
    port: "27017"
    # user: "Senbox"
    # password: ""
    name: "report_service"

consul:
    host: "localhost"
    port: 8500
    
registry:
  host: "localhost"

